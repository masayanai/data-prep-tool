# Generators

> **Classification**: Explainer / Directory Index — does not participate in
> conflict resolution.

This directory contains generator prompts that produce reusable or archived
artifacts.

- Role: `generator`
- Lifecycle: active reusable artifact
- Default load: no

Use these files only when a task explicitly requires generating one of the
document artifacts they describe.
