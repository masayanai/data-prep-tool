# Templates

> **Classification**: Explainer / Directory Index — does not participate in
> conflict resolution.

This directory contains reusable artifact templates.

- Role: `template`
- Lifecycle: active reusable artifact
- Default load: no

Use these files only when creating new reusable or generated artifacts.
Do not treat them as stable boot entrypoints.
